{# twig template #}
{% macro get_logo(classes = '', styles = '') %}
    <img id="logo" class="img-fluid {{ classes }}" src="{{ asset('build/images/logo.png') }}" alt="person cleaning gutters with vacuum" style="{{ styles }}"/>
{% endmacro %}